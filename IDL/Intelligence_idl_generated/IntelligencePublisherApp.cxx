// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file IntelligencePublisherApp.cxx
 * This file contains the implementation of the publisher functions.
 *
 * This file was generated by the tool fastddsgen.
 */

#include "IntelligencePublisherApp.hpp"

#include <condition_variable>
#include <csignal>
#include <stdexcept>
#include <thread>

#include <fastdds/dds/domain/DomainParticipantFactory.hpp>
#include <fastdds/dds/log/Log.hpp>
#include <fastdds/dds/publisher/DataWriter.hpp>
#include <fastdds/dds/publisher/Publisher.hpp>
#include <fastdds/dds/publisher/qos/DataWriterQos.hpp>
#include <fastdds/dds/publisher/qos/PublisherQos.hpp>

#include "IntelligencePubSubTypes.hpp"
#include <filesystem>
#include <unistd.h>
#include <cstring>
#include <nlohmann/json.hpp>
#include <fstream>
#include <iostream>
#include <vector>

using namespace eprosima::fastdds::dds;

IntelligencePublisherApp::IntelligencePublisherApp(
        const int& domain_id)
    : factory_(nullptr)
    , participant_(nullptr)
    , publisher_(nullptr)
    , topic_(nullptr)
    , writer_(nullptr)
    , type_(new Intelligence::FlatIntelligencePubSubType())
    , matched_(0)
    , samples_sent_(0)
    , stop_(false)
{
    //
    // Resolve DDS root directory dynamically (portable across PCs)
    auto resolve_dds_root = []() -> std::filesystem::path {
        if (const char* env_root = std::getenv("DDS_ROOT")) {
            std::filesystem::path p(env_root);
            if (std::filesystem::exists(p)) return p;
        }
        // Walk upwards from current directory to find repo root (has secure_dds and IDL)
        std::filesystem::path cur = std::filesystem::current_path();
        for (int i = 0; i < 6 && !cur.empty(); ++i) {
            if (std::filesystem::exists(cur / "secure_dds") && std::filesystem::exists(cur / "IDL")) {
                return cur;
            }
            cur = cur.parent_path();
        }
        return std::filesystem::current_path();
    };
    
    // Detect hostname dynamically (portable across PCs)
    char hostname[256] = {0};
    if (gethostname(hostname, sizeof(hostname) - 1) != 0) {
        std::strcpy(hostname, "UNKNOWN_HOST");
    }
    std::string participant_dir = std::string(hostname);
    
    const std::filesystem::path dds_root = resolve_dds_root();

    // Create the participant
    DomainParticipantQos pqos = PARTICIPANT_QOS_DEFAULT;
    // DDS Security Configuration - Authentication + Encryption Only
    pqos.properties().properties().emplace_back("dds.sec.auth.plugin", "builtin.PKI-DH");
    // Access Control disabled to avoid permissions issues
    pqos.properties().properties().emplace_back("dds.sec.crypto.plugin", "builtin.AES-GCM-GMAC");
    
    // Certificate and Key Paths (dynamic, portable across PCs)
    const std::string uri_prefix = "file://";
    const std::filesystem::path ca_path = dds_root / "secure_dds/CA/mainca_cert.pem";
    const std::filesystem::path cert_path = dds_root / "secure_dds/participants" / participant_dir / (participant_dir + "_cert.pem");
    const std::filesystem::path key_path = dds_root / "secure_dds/participants" / participant_dir / (participant_dir + "_key.pem");
    
    pqos.properties().properties().emplace_back("dds.sec.auth.builtin.PKI-DH.identity_ca", uri_prefix + ca_path.string());
    pqos.properties().properties().emplace_back("dds.sec.auth.builtin.PKI-DH.identity_certificate", uri_prefix + cert_path.string());
    pqos.properties().properties().emplace_back("dds.sec.auth.builtin.PKI-DH.private_key", uri_prefix + key_path.string());
    
    // Access Control disabled - no governance/permissions needed
    pqos.name("Intelligence::FlatIntelligence_pub_participant");
    factory_ = DomainParticipantFactory::get_shared_instance();
    participant_ = factory_->create_participant(domain_id, pqos, nullptr, StatusMask::none());
    if (participant_ == nullptr)
    {
        throw std::runtime_error("Intelligence::FlatIntelligence Participant initialization failed");
    }

    // Register the type
    type_.register_type(participant_);

    // Create the publisher
    PublisherQos pub_qos = PUBLISHER_QOS_DEFAULT;
    participant_->get_default_publisher_qos(pub_qos);
    publisher_ = participant_->create_publisher(pub_qos, nullptr, StatusMask::none());
    if (publisher_ == nullptr)
    {
        throw std::runtime_error("Intelligence::FlatIntelligence Publisher initialization failed");
    }

    // Create the topic
    TopicQos topic_qos = TOPIC_QOS_DEFAULT;
    participant_->get_default_topic_qos(topic_qos);
    topic_ = participant_->create_topic("IntelligenceTopic", type_.get_type_name(), topic_qos);
    if (topic_ == nullptr)
    {
        throw std::runtime_error("Intelligence::FlatIntelligence Topic initialization failed");
    }

    // Create the data writer
    DataWriterQos writer_qos = DATAWRITER_QOS_DEFAULT;
    // Payload Encryption Configuration
    writer_qos.properties().properties().emplace_back("rtps.payload_protection", "ENCRYPT");
    publisher_->get_default_datawriter_qos(writer_qos);
    writer_qos.reliability().kind = ReliabilityQosPolicyKind::RELIABLE_RELIABILITY_QOS;
    writer_qos.durability().kind = DurabilityQosPolicyKind::TRANSIENT_LOCAL_DURABILITY_QOS;
    writer_qos.history().kind = HistoryQosPolicyKind::KEEP_ALL_HISTORY_QOS;
    writer_ = publisher_->create_datawriter(topic_, writer_qos, this, StatusMask::all());
    if (writer_ == nullptr)
    {
        throw std::runtime_error("Intelligence::FlatIntelligence DataWriter initialization failed");
    }
}

IntelligencePublisherApp::~IntelligencePublisherApp()
{
    if (nullptr != participant_)
    {
        // Delete DDS entities contained within the DomainParticipant
        participant_->delete_contained_entities();

        // Delete DomainParticipant
        factory_->delete_participant(participant_);
    }
}

void IntelligencePublisherApp::on_publication_matched(
        DataWriter* /*writer*/,
        const PublicationMatchedStatus& info)
{
    if (info.current_count_change == 1)
    {
        {
            std::lock_guard<std::mutex> lock(mutex_);
            matched_ = info.current_count;
        }
        std::cout << "Intelligence::FlatIntelligence Publisher matched." << std::endl;
        cv_.notify_one();
    }
    else if (info.current_count_change == -1)
    {
        {
            std::lock_guard<std::mutex> lock(mutex_);
            matched_ = info.current_count;
        }
        std::cout << "Intelligence::FlatIntelligence Publisher unmatched." << std::endl;
    }
    else
    {
        std::cout << info.current_count_change
                  << " is not a valid value for PublicationMatchedStatus current count change" << std::endl;
    }
}

void IntelligencePublisherApp::run()
{
    while (!is_stopped())
    {
        if (publish())
        {
            std::cout << "Sample '" << std::to_string(++samples_sent_) << "' SENT" << std::endl;
        }
        // Wait for period or stop event
        std::unique_lock<std::mutex> period_lock(mutex_);
        cv_.wait_for(period_lock, std::chrono::milliseconds(period_ms_), [this]()
                {
                    return is_stopped();
                });
    }
}


bool IntelligencePublisherApp::publish()
{
    bool ret = false;
    // Wait for the data endpoints discovery
    std::unique_lock<std::mutex> matched_lock(mutex_);
    cv_.wait(matched_lock, [&]()
            {
                // at least one has been discovered
                return ((matched_ > 0) || is_stopped());
            });

    if (!is_stopped())
    {
        /* Initialize your structure here */
        Intelligence::FlatIntelligence sample_;
        // --- BEGIN AUTOGENERATED IDL PATCH (v9) - JSON ENHANCED ---
        // JSON data reading setup
        static std::vector<nlohmann::json> json_data;
        static size_t current_index = 0;
        static bool json_loaded = false;
        
        if (!json_loaded) {
            // Try multiple possible paths for the JSON file
            std::vector<std::string> possible_paths = {
                "scenarios/Intelligence.json",
                "../../scenarios/Intelligence.json",
                "../../../scenarios/Intelligence.json"
            };
            
            bool file_found = false;
            for (const auto& path : possible_paths) {
                std::ifstream json_file(path);
                if (json_file.is_open()) {
                    file_found = true;
                    nlohmann::json j;
                    json_file >> j;
                    if (j.is_array()) {
                        json_data = j.get<std::vector<nlohmann::json>>();
                    } else {
                        json_data.push_back(j);
                    }
                    json_loaded = true;
                    std::cout << "Loaded " << json_data.size() << " entries from " << path << std::endl;
                    break;
                }
            }
            
            if (!file_found) {
                std::cerr << "Warning: Could not find Intelligence.json in any expected location, using default values" << std::endl;
            }
        }
        
        // Use JSON data if available, otherwise use defaults
        if (!json_data.empty()) {
            const auto& current_data = json_data[current_index % json_data.size()];
            
            // Set values from JSON data
            if (current_data.contains("vs_task_status")) {
                sample_.vs_task_status(current_data["vs_task_status"].get<int>());
            } else {
                sample_.vs_task_status(90);
            }
            if (current_data.contains("vs_battery_percentage")) {
                sample_.vs_battery_percentage(current_data["vs_battery_percentage"].get<int>());
            } else {
                sample_.vs_battery_percentage(80);
            }
            if (current_data.contains("vs_signal_strength_dbm")) {
                sample_.vs_signal_strength_dbm(current_data["vs_signal_strength_dbm"].get<float>());
            } else {
                sample_.vs_signal_strength_dbm(-70.0f);
            }
            if (current_data.contains("vs_system_error")) {
                sample_.vs_system_error(current_data["vs_system_error"].get<float>());
            } else {
                sample_.vs_system_error(false);
            }
            if (current_data.contains("td_target_ID")) {
                sample_.td_target_ID(current_data["td_target_ID"].get<std::string>());
            } else {
                sample_.td_target_ID("Target_789");
            }
            if (current_data.contains("td_target_type")) {
                sample_.td_target_type(current_data["td_target_type"].get<int>());
            } else {
                sample_.td_target_type(90);
            }
            if (current_data.contains("td_location_latitude")) {
                sample_.td_location_latitude(current_data["td_location_latitude"].get<float>());
            } else {
                sample_.td_location_latitude(37.7749);
            }
            if (current_data.contains("td_location_longitude")) {
                sample_.td_location_longitude(current_data["td_location_longitude"].get<float>());
            } else {
                sample_.td_location_longitude(-122.4194);
            }
            if (current_data.contains("td_location_altitude")) {
                sample_.td_location_altitude(current_data["td_location_altitude"].get<float>());
            } else {
                sample_.td_location_altitude(100.0f);
            }
            if (current_data.contains("td_location_time_seconds")) {
                sample_.td_location_time_seconds(current_data["td_location_time_seconds"].get<long>());
            } else {
                sample_.td_location_time_seconds(1678886400L);
            }
            if (current_data.contains("td_location_time_nano_seconds")) {
                sample_.td_location_time_nano_seconds(current_data["td_location_time_nano_seconds"].get<long>());
            } else {
                sample_.td_location_time_nano_seconds(1678886400L);
            }
            if (current_data.contains("td_location_speed_mps")) {
                sample_.td_location_speed_mps(current_data["td_location_speed_mps"].get<float>());
            } else {
                sample_.td_location_speed_mps(15.5f);
            }
            if (current_data.contains("td_location_orientation_degrees")) {
                sample_.td_location_orientation_degrees(current_data["td_location_orientation_degrees"].get<int>());
            } else {
                sample_.td_location_orientation_degrees(180);
            }
            if (current_data.contains("td_confidence_level")) {
                sample_.td_confidence_level(current_data["td_confidence_level"].get<float>());
            } else {
                sample_.td_confidence_level(0.95f);
            }
            if (current_data.contains("td_description")) {
                sample_.td_description(current_data["td_description"].get<std::string>());
            } else {
                sample_.td_description("Detected person of interest");
            }
            if (current_data.contains("td_raw_data_link")) {
                sample_.td_raw_data_link(current_data["td_raw_data_link"].get<std::string>());
            } else {
                sample_.td_raw_data_link("./data/raw.bin");
            }
            if (current_data.contains("ta_command")) {
                sample_.ta_command(current_data["ta_command"].get<std::string>());
            } else {
                sample_.ta_command("Patrol");
            }
            if (current_data.contains("ta_location_latitude")) {
                sample_.ta_location_latitude(current_data["ta_location_latitude"].get<float>());
            } else {
                sample_.ta_location_latitude(37.7749);
            }
            if (current_data.contains("ta_location_longitude")) {
                sample_.ta_location_longitude(current_data["ta_location_longitude"].get<float>());
            } else {
                sample_.ta_location_longitude(-122.4194);
            }
            if (current_data.contains("ta_location_altitude")) {
                sample_.ta_location_altitude(current_data["ta_location_altitude"].get<float>());
            } else {
                sample_.ta_location_altitude(100.0f);
            }
            if (current_data.contains("ta_location_time_seconds")) {
                sample_.ta_location_time_seconds(current_data["ta_location_time_seconds"].get<long>());
            } else {
                sample_.ta_location_time_seconds(1678886400L);
            }
            if (current_data.contains("ta_location_time_nano_seconds")) {
                sample_.ta_location_time_nano_seconds(current_data["ta_location_time_nano_seconds"].get<long>());
            } else {
                sample_.ta_location_time_nano_seconds(1678886400L);
            }
            if (current_data.contains("ta_location_speed_mps")) {
                sample_.ta_location_speed_mps(current_data["ta_location_speed_mps"].get<float>());
            } else {
                sample_.ta_location_speed_mps(15.5f);
            }
            if (current_data.contains("ta_location_orientation_degrees")) {
                sample_.ta_location_orientation_degrees(current_data["ta_location_orientation_degrees"].get<int>());
            } else {
                sample_.ta_location_orientation_degrees(180);
            }
            
            // Move to next data entry for next publish
            current_index++;
        } else {
            // Fallback to original hardcoded values
            sample_.vs_task_status(90);
            sample_.vs_battery_percentage(80);
            sample_.vs_signal_strength_dbm(-70.0f);
            sample_.vs_system_error(false);
            sample_.td_target_ID("Target_789");
            sample_.td_target_type(90);
            sample_.td_location_latitude(37.7749);
            sample_.td_location_longitude(-122.4194);
            sample_.td_location_altitude(100.0f);
            sample_.td_location_time_seconds(1678886400L);
            sample_.td_location_time_nano_seconds(1678886400L);
            sample_.td_location_speed_mps(15.5f);
            sample_.td_location_orientation_degrees(180);
            sample_.td_confidence_level(0.95f);
            sample_.td_description("Detected person of interest");
            sample_.td_raw_data_link("./data/raw.bin");
            sample_.ta_command("Patrol");
            sample_.ta_location_latitude(37.7749);
            sample_.ta_location_longitude(-122.4194);
            sample_.ta_location_altitude(100.0f);
            sample_.ta_location_time_seconds(1678886400L);
            sample_.ta_location_time_nano_seconds(1678886400L);
            sample_.ta_location_speed_mps(15.5f);
            sample_.ta_location_orientation_degrees(180);
        }
        // --- END AUTOGENERATED IDL PATCH (v9) - JSON ENHANCED ---

        ret = (RETCODE_OK == writer_->write(&sample_));
    }
    return ret;
}

bool IntelligencePublisherApp::is_stopped()
{
    return stop_.load();
}

void IntelligencePublisherApp::stop()
{
    stop_.store(true);
    cv_.notify_one();
}